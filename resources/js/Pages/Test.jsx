import { Navbar } from '../Components'
import { Head } from '@inertiajs/react';

export default function Dashboard({ auth }) {
    return (
        <div className='flex'>
            <Navbar />
        </div>
    );
}
